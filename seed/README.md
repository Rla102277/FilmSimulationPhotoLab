# Seed snapshots

These are convenience snapshots for development. They are not authoritative. The immutable Leica archive is authoritative and API/runtime code should verify/read it directly until an explicit migration process is approved.
