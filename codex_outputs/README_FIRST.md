# Historical Codex outputs

This is the complete user-facing output archive from the local Codex project.
It intentionally includes successful deliverables, failed probes, intermediate
experiments, settings backups and packet captures so no evidence is lost.

Use `../reference/leica/v1.2/Infinite_Arch_Leica_Looks_v1.2.zip` as ground truth.
Do not run the older Bank A/Bank B installers or assume every historical package
is production-ready.

The packet captures and settings files may expose camera identifiers and local
network details. Keep the Repl private.

